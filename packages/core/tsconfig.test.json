{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests may reach for platform globals (TextEncoder, timers) that src must
    // not touch.
    "lib": ["ES2022", "DOM"],
    "types": ["node"],
    "tsBuildInfoFile": "./node_modules/.tmp/core-test.tsbuildinfo"
  },
  "include": ["src", "test"]
}
